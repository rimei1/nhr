<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Field4__c</fullName>
    <formula>AND(
  ISBLANK(tyakkoubi__c), 
  DATEVALUE(CreatedDate) &gt;= ADDMONTHS(DATE(YEAR(TODAY()), MONTH(TODAY()), 1), -1), 
  DATEVALUE(CreatedDate) &lt;  ADDMONTHS(DATE(YEAR(TODAY()), MONTH(TODAY()), 1),  1) )</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>着工日未入力数式</label>
    <trackHistory>false</trackHistory>
    <type>Checkbox</type>
</CustomField>
